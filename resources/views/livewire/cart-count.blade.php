<span class="relative inline-block">
    @if ($count)
        <span class="-top-2 -right-4 bg-red-600 text-white text-xs font-bold px-2 py-0.5 rounded-full">
            {{ $count }}
        </span>
    @else
        <span class="-top-2 -right-4 bg-red-600 text-white text-xs font-bold px-2 py-0.5 rounded-full">
            {{ $count }}
        </span>
    @endif
</span>
